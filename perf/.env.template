# cp .env.template .env && source .env
# Ne JAMAIS cibler la production.

export API_URL="http://localhost:5000"

# --- LoginEtAccueilFranceTravailSimulation (login réel via connect) ---
# Les clients OIDC de connect sont pilotés par variables d'environnement :
# reprendre CLIENT_APP_ID / CLIENT_APP_SECRET et un des CLIENT_APP_CALLBACKS
# de l'app connect ciblée.
export CONNECT_URL="http://localhost:5050"
export CLIENT_ID=""
export CLIENT_SECRET=""
export REDIRECT_URI=""
# export KC_IDP_HINT=pe-jeune

# --- AccueilFranceTravailSimulation (JWT fourni à la main) ---
export USER_ID="ca5014cb-9bb5-4d8e-befb-596ee59c4d7b"  # Alban336 (jeune FT)
export USER_TOKEN=""                                   # JWT du jeune, non expiré

# export MAX_USERS=10
# export RAMP_DURATION_IN_SECONDS=30
# export HOLD_DURATION_IN_SECONDS=60
# export P95_THRESHOLD_MS=5000
# export FAILED_PERCENT_THRESHOLD=1.0
